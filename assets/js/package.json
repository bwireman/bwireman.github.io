{
  "dependencies": {
    "bulma": "^0.9.3",
    "esbuild": "^0.14.29"
  },
  "devDependencies": {
    "esbuild-sass-plugin": "^2.2.6",
    "esgleam": "^0.2.0"
  },
  "scripts": {
    "build": "rm -rf out && gleam clean && node build.mjs"
  }
}
